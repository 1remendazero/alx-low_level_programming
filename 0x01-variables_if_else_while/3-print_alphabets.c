#include <stdio.h>
#include <stdlib.h>

/**
 *
 * Return - returnsalphabets both lower and upper cases
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int ch = 'a';
	int ch = 'A';

	while (ch <= 'z')
	{
		putchar(ch)
		ch++;
	}
	while (CH <= 'Z')
	{PUTCHAR(ch);
		CH++;
	}

	PUTCHAR('\n');
	return (0);
